import Image from "next/image";
import { ArrowUpRight } from "lucide-react";

type ProjectCardProps = { title: string; description: string; image: string; tags: string[]; demoUrl: string; codeUrl?: string; index: number };

export default function ProjectCard({ title, description, image, tags, demoUrl, codeUrl, index }: ProjectCardProps) { return <article className="group overflow-hidden rounded-[1.75rem] border border-white/10 bg-[#17252d] transition duration-500 hover:-translate-y-1 hover:border-white/25"><div className="relative aspect-[16/9] overflow-hidden"><Image src={image || "/placeholder.svg"} alt={title} fill className="object-cover transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#101a20] via-transparent to-transparent opacity-80" /><span className="absolute left-5 top-5 rounded-full border border-white/20 bg-black/20 px-3 py-1 text-xs text-white/75 backdrop-blur-md">0{index + 1}</span></div><div className="p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><h3 className="text-2xl text-white">{title}</h3><p className="mt-2 text-sm leading-relaxed text-white/55 sm:text-base">{description}</p></div><a href={demoUrl} target="_blank" rel="noopener noreferrer" aria-label={`Apri ${title}`} className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-white/15 text-white transition hover:bg-white hover:text-[#17252d]"><ArrowUpRight className="h-4 w-4" /></a></div><div className="mt-6 flex flex-wrap gap-2">{tags.map((tag) => <span key={tag} className="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-white/60">{tag}</span>)}</div><div className="mt-7 flex gap-3"><a href={demoUrl} target="_blank" rel="noopener noreferrer" className="rounded-full bg-white px-5 py-2.5 text-sm font-medium text-[#172127] transition hover:bg-[#dbe8eb]">Demo Live</a>{codeUrl && <a href={codeUrl} target="_blank" rel="noopener noreferrer" className="rounded-full border border-white/20 px-5 py-2.5 text-sm font-medium text-white transition hover:bg-white hover:text-[#172127]">Vedi Codice</a>}</div></div></article>; }
